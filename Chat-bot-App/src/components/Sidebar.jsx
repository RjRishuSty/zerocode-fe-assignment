import { Stack } from "@mui/material";
import React from "react";

const Sidebar = () => {
  return (
    <Stack
      sx={{border:'2px solid red', minHeight: "100vh", backgroundColor: "background.paper" }}
    ></Stack>
  );
};

export default Sidebar;
